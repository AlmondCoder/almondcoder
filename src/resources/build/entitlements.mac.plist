<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow file system read/write access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow full disk access -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Allow access to user data -->
    <key>com.apple.security.personal-information.location</key>
    <false/>

    <!-- Allow network access for Claude CLI -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow outgoing network connections -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Disable app sandbox to allow file system access -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- Allow arbitrary loads for Claude CLI -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow spawning child processes -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow loading unsigned libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>